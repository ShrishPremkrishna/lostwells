# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Node / Next.js ---
node_modules/
.next/
out/
# generated copy of data/processed (source of truth lives in data/processed)
apps/web/public/data/
.turbo/
*.tsbuildinfo
npm-debug.log*
.vercel/

# --- Env / secrets ---
.env
.env.local
.env*.local
*.key

# --- Data: commit processed datastore, ignore bulky raw + caches ---
# (covers data/raw/htmc/*.csv USGS HTMC inventory + data/raw/tiger/*.zip county/tract caches)
data/raw/
data/cache/
# Full per-well scored economics (>100MB, exceeds GitHub's limit). Regenerable
# via services/engine/score_candidates.py; the web app reads the slim
# candidates.web.json + detail/NN.json shards, not this intermediate.
data/processed/candidates.scored.json
*.tif
*.tiff
*.zip
*.gpkg
# Large model weights (U-Net) are never committed
models/
*.h5
*.keras

# --- U-Net research scratch (agent's local download of the LBNL EDX dataset) ---
catalog_research/

# --- OS ---
.DS_Store
Thumbs.db
.gstack/
